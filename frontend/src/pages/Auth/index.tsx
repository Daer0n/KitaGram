import { Container } from './styled';
import { AuthForm } from '@components/AuthForm';

export const Auth = () => {
    return (
        <Container>
            <AuthForm />
        </Container>
    );
};
