import { Container } from './styled';
import { Login } from '@components/Login';

export const Auth = () => {
    return (
        <Container>
            <Login></Login>
        </Container>
    );
};
