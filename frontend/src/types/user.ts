export type User = {
    deviceToken: string;
    email: string;
    nickname: string;
    password: string;
    registrationToken: string;
    surname: string;
    telephoneNumber: string;
    username: string;
};
